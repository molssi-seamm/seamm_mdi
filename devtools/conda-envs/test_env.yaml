name: test

channels:
  - conda-forge

dependencies:
  # Base depends
  - python
  - pip

  - numpy
  - seamm-util

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov

  # Documentation
  - pydata-sphinx-theme
  - sphinx-design

  # Pip-only installs
  - pip:
      - pymdi
      # Documentation
      - sphinx-copybutton
